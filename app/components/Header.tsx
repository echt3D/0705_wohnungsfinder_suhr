import Option from "./Option";

const Header = () => {
  return (
    <header className="bg-white px-12 flex justify-end items-center h-header_desktop">
      <Option />
    </header>
  );
};

export default Header;
